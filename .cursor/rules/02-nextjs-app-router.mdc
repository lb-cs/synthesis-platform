---
globs: app/**/*.ts,app/**/*.tsx,components/**/*.tsx
description: Next.js 16 App Router — server vs client components, route handlers, env, generated files
---

See @docs/rules/02-nextjs-app-router.md for the full rules.

Content lives in `docs/rules/` so every agent reads the same source. Edit it there,
not here — this file only carries the glob scoping.
